build:
  image: node
  commands:
    - npm install -g angular-cli
    - npm install
    - ng test

publish:
  docker:
    username: $$DOCKER_USER
    password: $$DOCKER_PASSWORD
    email: $$DOCKER_EMAIL
    repo: cp2017/webapp
    tag: latest
    file: Dockerfile
    insecure: false
